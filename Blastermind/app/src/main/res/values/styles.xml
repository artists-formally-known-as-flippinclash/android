<resources>

    <style name="AppTheme" parent="@style/Theme.AppCompat.Light.DarkActionBar">
        <item name="colorPrimary">@color/material_dark</item>
        <!--<item name="colorPrimaryDark">@color/primary_dark</item>-->
        <item name="android:textColorPrimary">@color/light_text</item>
        <!--<item name="colorAccent">@color/light_text</item>-->
    </style>


    <style name="SquareButton" parent="android:Widget.Button">
        <!-- height = width -->
        <item name="android:minHeight">3dip</item>
        <item name="android:minWidth">3dip</item>
    </style>

    <style name="CustomEditText" parent="@style/Base.Widget.AppCompat.EditText">
        <item name="android:textColor">@color/light_text</item>
        <item name="android:textColorHint">@color/light_text</item>
    </style>

</resources>
